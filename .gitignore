*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Native libraries are downloaded at build time (CI) or dropped in manually
app/src/main/jniLibs/

# Release signing credentials must never be committed
*.keystore
keystore.properties
